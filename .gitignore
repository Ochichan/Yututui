/target
/dist

# Frontend workspace: deps and build output are never vendored (package-lock.json is
# the reproducibility stance; gui/src/generated/ IS committed and drift-gated).
gui/node_modules/
gui/dist/

# Browser cookie exports (login secrets — never commit)
cookies.txt
*cookies*.txt
# Nix build symlinks (`nix build` writes ./result)
/result
/result-*
/YtmTui.app/
/ytm-tui-*.tar.gz
/ytm-tui-*.zip
/ytm-tui icons/
.DS_Store
.claude/

# Local one-off helper scripts
/local-*.bat
/local-*.cmd
/local-*.ps1
/local-*.sh
/scratch-*.bat
/scratch-*.cmd
/scratch-*.ps1
/scratch-*.sh
/tmp-*.bat
/tmp-*.cmd
/tmp-*.ps1
/tmp-*.sh
/adhoc-*.bat
/adhoc-*.cmd
/adhoc-*.ps1
/adhoc-*.sh
/oneoff-*.bat
/oneoff-*.cmd
/oneoff-*.ps1
/oneoff-*.sh

# Local docs notes
/ARCHITECTURE.md
/docs/daemon-mode-completion-plan.md
/docs/desktop-tray-companion-plan.md
/docs/*.md
/docs/**/*.md
# Full-GUI design set — kept as local working notes (see docs/gui/README.md)
/docs/gui/
*.sha256

# Fable Harness (local agent harness — kept out of the repo per 2026-07-06 decision)
/CLAUDE.md
/AGENTS.md
/.claude/settings.json
/.claude/hooks/
/.claude/harness/
/.agents/
/.codex/
/.codex-worktrees/

# Cursor IDE / debug session artifacts
.cursor/
